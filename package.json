{
    "name": "@smooai/smooth-operator-core-monorepo",
    "version": "0.0.0",
    "private": true,
    "description": "Polyglot AI agent orchestration core — agents, workflows, tools, checkpointing, memory, HITL, cost. The engine behind smooth-agent and lom.smoo.ai.",
    "license": "MIT",
    "repository": {
        "type": "git",
        "url": "git+https://github.com/SmooAI/smooth-operator-core.git"
    },
    "homepage": "https://github.com/SmooAI/smooth-operator-core",
    "bugs": {
        "url": "https://github.com/SmooAI/smooth-operator-core/issues"
    },
    "type": "module",
    "scripts": {
        "rust:check": "cd rust && cargo check --workspace",
        "rust:fmt": "cd rust && cargo fmt --all",
        "rust:fmt:check": "cd rust && cargo fmt --all -- --check",
        "rust:clippy": "cd rust && cargo clippy --workspace --all-targets -- -D warnings",
        "rust:test": "cd rust && cargo test --workspace",
        "changeset": "changeset",
        "ci:publish": "changeset publish"
    },
    "devDependencies": {
        "@changesets/cli": "^2.27.10"
    },
    "packageManager": "pnpm@10.6.1",
    "engines": {
        "node": ">=22"
    }
}
