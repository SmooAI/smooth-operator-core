{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://smooth-agent.dev/spec/events/write-confirmation-required.schema.json",
  "title": "WriteConfirmationRequired",
  "description": "Event: `write_confirmation_required`. Emitted when the agent workflow pauses before running a state-mutating tool call that requires explicit user approval. Corresponds to smooth-operator's `AgentEvent::HumanInputRequired { Confirm }`. The client must surface a confirmation dialog and reply with a `confirm_tool_action` action using the same `requestId`. Until the client responds, the workflow remains paused.",
  "type": "object",
  "required": ["type", "data"],
  "additionalProperties": false,
  "properties": {
    "type": {
      "type": "string",
      "const": "write_confirmation_required",
      "description": "Event type discriminator."
    },
    "requestId": {
      "type": "string",
      "description": "Echoes the `requestId` from the originating `send_message` action. Must be included in the `confirm_tool_action` reply."
    },
    "data": {
      "type": "object",
      "description": "Confirmation prompt details.",
      "required": ["requestId", "data"],
      "additionalProperties": false,
      "properties": {
        "requestId": {
          "type": "string",
          "description": "The request ID this confirmation belongs to."
        },
        "data": {
          "type": "object",
          "description": "Details about the pending write that the user must approve or reject.",
          "required": ["toolId", "actionDescription"],
          "additionalProperties": false,
          "properties": {
            "toolId": {
              "type": "string",
              "description": "Opaque identifier of the tool invocation awaiting confirmation. Provided for correlation; clients do not need to parse or display this."
            },
            "actionDescription": {
              "type": "string",
              "description": "Human-readable description of the action the agent wants to perform, suitable for displaying in a confirmation dialog (e.g. `Delete contact John Doe (john@example.com)`)."
            }
          }
        }
      }
    },
    "timestamp": {
      "type": "integer",
      "description": "Unix epoch milliseconds when the event was emitted."
    }
  }
}
